name: build

on:
  pull_request:
    branches: [ master ]

jobs:

  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

  build:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: jkstack/smartagent-build@1.19.1